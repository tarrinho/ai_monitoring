apiVersion: v2
name: ai-monitoring
description: Read-only monitor for a local LLM stack (host / GPU / Ollama / llama.cpp / LiteLLM / containers) with a Prometheus /metrics endpoint for fleet aggregation.
type: application
version: 1.6.2          # chart version — keep in step with the app version
appVersion: "1.6.2"    # AI-Monitoring image tag
home: https://github.com/tarrinho/ai_monitoring
sources:
  - https://github.com/tarrinho/ai_monitoring
keywords:
  - llm
  - monitoring
  - gpu
  - prometheus
  - litellm
  - ollama
maintainers:
  - name: Pedro Tarrinho
